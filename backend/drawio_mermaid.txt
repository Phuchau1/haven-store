erDiagram
    Address {
        string _id
        varchar id
        varchar user_id
        varchar full_name
        varchar phone
        varchar city
        varchar district
        varchar ward
        varchar street
        boolean is_default
        timestamp created_at
    }
    AISetting {
        string _id
    }
    Article {
        string _id
    }
    AuditLog {
        string _id
        varchar id
        varchar user_id
        varchar action
        varchar entity_type
        varchar entity_id
        varchar old_values
        varchar new_values
        varchar ip_address
        timestamp created_at
    }
    Banner {
        string _id
        varchar id
        varchar title
        varchar image
        varchar video
        varchar link
        varchar status
        timestamp created_at
    }
    Cart {
        string _id
        varchar variant_id
        int quantity
        int price
        varchar id
        varchar user_id
        timestamp created_at
    }
    Category {
        string _id
        varchar id
        varchar name
        varchar slug
        varchar description
        varchar image
        int order
        boolean isActive
        varchar id
        varchar name
        varchar description
        varchar image
        varchar video
        int count
        int order
        boolean isActive
        varchar subcategories
    }
    ChatMessage {
        string _id
        varchar id
        varchar session_id
        varchar sender_type
        varchar message
    }
    ChatSession {
        string _id
        varchar id
        varchar customer_name
        varchar phone
        varchar userId
        varchar status
    }
    Color {
        string _id
        varchar id
        varchar name
        varchar code
    }
    Coupon {
        string _id
        varchar id
        varchar code
        varchar discount_type
        int discount_value
        varchar start_date
        varchar end_date
        int usage_limit
        int usage_limit_per_user
    }
    FlashSale {
        string _id
    }
    InventoryHistory {
        string _id
        varchar id
        varchar variant_id
        varchar type
        int quantity
        varchar note
        timestamp created_at
    }
    LuckyWheelConfig {
        string _id
    }
    Menu {
        string _id
        varchar id
        varchar title
        varchar link
        int order
        boolean isActive
        varchar children
    }
    Order {
        string _id
        varchar id
        varchar name
        int price
        int originalPrice
        varchar category
        varchar categoryLabel
        array images
        array sizes
        array colors
        varchar name
        varchar hex
        varchar description
        varchar badge
        int rating
        int reviews
        boolean inStock
        varchar product
        int quantity
        varchar selectedSize
        varchar selectedColor
        varchar name
        varchar hex
        varchar id
        varchar customerName
        varchar phone
        varchar email
        varchar address
        varchar paymentMethod
        int totalAmount
        varchar couponCode
        int discountAmount
        int finalAmount
        varchar note
        varchar transferReceipt
        varchar shippingProvider
        varchar status
        varchar type
        varchar createdAt
    }
    OrderStatusHistory {
        string _id
        varchar id
        varchar order_id
        varchar status
        varchar note
        timestamp created_at
    }
    PaymentMethod {
        string _id
        varchar id
        varchar name_methond
        varchar description
        varchar bank_info
        varchar qr_code_url
        boolean is_active
    }
    Product {
        string _id
        varchar name
        varchar hex
        varchar image
        varchar color
        varchar size
        int stock
        int price
        int originalPrice
        varchar id
        varchar name
        int price
        int originalPrice
        varchar category
        varchar categoryLabel
        json category_id
        varchar subCategory
        varchar subCategoryLabel
        array images
        array sizes
        varchar description
        varchar content
        varchar shortDescription
        varchar richContent
        json specifications
        array sizeGuide
        array careInstructions
        array features
        array tags
        json seo
        array faqs
        varchar question
        varchar answer
        array certificates
        array fabric
        varchar status
        timestamp publishAt
        array videos
        array instructions
        array notes
        varchar sizeChartImage
        varchar badge
        int rating
        int reviews
        boolean inStock
        int soldQuantity
    }
    ProductReview {
        string _id
        varchar id
        varchar user_id
        varchar userName
        varchar userEmail
        varchar product_id
        int rating
        varchar content
        varchar status
        timestamp created_at
    }
    ProductVariant {
        string _id
        varchar id
        varchar product_id
        varchar size_id
        varchar color_id
        int stock
        int reserved_stock
        array warehouse_stocks
        varchar warehouse_id
        int stock
        varchar barcode
        varchar qr_code
        varchar image
        int price
        varchar sku
        varchar status
    }
    PurchaseOrder {
        string _id
        varchar variant_id
        int quantity
        int price
        varchar id
        varchar supplier_id
        varchar warehouse_id
        varchar status
        varchar user_id
        varchar expected_date
        int total_amount
        varchar note
    }
    Review {
        string _id
        varchar productId
        varchar userId
        varchar customerName
        int rating
        varchar comment
        boolean isApproved
    }
    ShippingMethod {
        string _id
        varchar id
        varchar name_methond
        varchar description
        boolean is_active
    }
    SiteSetting {
        string _id
    }
    Size {
        string _id
        varchar id
        varchar name
    }
    SpinHistory {
        string _id
    }
    SpinReward {
        string _id
    }
    StockReceipt {
        string _id
        varchar variant_id
        int quantity
        int price
        varchar id
        varchar type
        varchar warehouse_id
        varchar dest_warehouse_id
        varchar supplier_id
        varchar po_id
        varchar reason
        varchar note
        varchar user_id
        varchar status
        int total_quantity
        int total_amount
    }
    StockTransaction {
        string _id
        varchar id
        varchar type
        varchar reference_id
        varchar warehouse_id
        varchar variant_id
        int quantity
        int before_stock
        int after_stock
        varchar note
        varchar user_id
    }
    Supplier {
        string _id
        varchar id
        varchar name
        varchar email
        varchar phone
        varchar address
        varchar tax_code
        varchar status
    }
    User {
        string _id
        varchar id
        varchar name
        varchar email
        varchar password
        varchar googleId
        varchar facebookId
        varchar avatar
        varchar role
        varchar phone
        varchar address
        varchar resetPasswordToken
        timestamp resetPasswordExpires
    }
    UserCoupon {
        string _id
    }
    UserVoucher {
        string _id
        varchar id
        varchar user_id
        varchar coupon_id
        boolean is_use
    }
    Warehouse {
        string _id
        varchar id
        varchar name
        varchar code
        varchar address
        varchar manager_id
        varchar status
    }
    Wishlist {
        string _id
        varchar id
        varchar user_id
        varchar product_id
        timestamp created_at
    }

    Category ||--o{ Product : "has"
    Product ||--o{ OrderItem : "has"
    Order ||--o{ OrderItem : "has"
    Product ||--o{ Review : "has"
    User ||--o{ Review : "has"
    User ||--o{ Cart : "has"
    Cart ||--o{ CartItem : "has"
    Product ||--o{ CartItem : "has"
    Product ||--o{ ProductVariant : "has"
    User ||--o{ Wishlist : "has"
    Product ||--o{ Wishlist : "has"
    Product ||--o{ StockTransaction : "has"
    Supplier ||--o{ PurchaseOrder : "has"
    PurchaseOrder ||--o{ StockReceipt : "has"
    Order ||--o{ OrderStatusHistory : "has"
    Product ||--o{ ProductReview : "has"
    User ||--o{ UserVoucher : "has"
    ChatSession ||--o{ ChatMessage : "has"
